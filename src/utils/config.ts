export const PROMPT =
"Summarize the following text into 1 to 8 short main lines, prioritizing accuracy and relevance. Exclude any references to the news outlet, author, or unrelated stories. Return only the lines, one per line, with no other text. Do not begin any line with a dash/bullet character (e.g., '-', '–', '—', '•', '*') and do not number the lines. Do not end lines with punctuation.";;

export const OPENAI_MODEL = "gpt-5-nano";
export const GEMINI_MODEL = "gemini-2.5-flash-lite";
